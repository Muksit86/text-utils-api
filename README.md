# Text Utils API

This is a simple Express-based API with endpoints for:
- Word count
- Uppercase
- Whitespace cleaner
- Text reversal

## Example Request:
POST /api/wordcount  
`{ "text": "hello world" }`
